<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:id="@android:id/background">
        <shape>
            <corners android:radius="5dip" />
            <gradient
                android:startColor="@color/gray_main"
                android:centerColor="@color/gray_main"
                android:centerY="0.75"
                android:endColor="@color/gray_main"
                android:angle="270"
                />
        </shape>
    </item>

    <item android:id="@android:id/secondaryProgress">
        <clip>
            <shape>
                <corners android:radius="5dip" />
                <gradient
                    android:startColor="@color/color1_subject"
                    android:centerColor="@color/color1_subject"
                    android:centerY="0.75"
                    android:endColor="@color/color1_subject"
                    android:angle="270"
                    />
            </shape>
        </clip>
    </item>
    <item
        android:id="@android:id/progress"
        >
        <clip>
            <shape>
                <corners
                    android:radius="5dip" />
                <gradient
                    android:startColor="@color/color1_subject"
                    android:endColor="@color/color1_subject"
                    android:angle="270" />
            </shape>
        </clip>
    </item>

</layer-list>